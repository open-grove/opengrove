# Bundled Skills

This directory is reserved for official OpenGrove skills that should ship with the npm package.

User or project skills belong in `.opengrove/skills`, `.claude/skills`, `.codex/skills`, or the matching home-directory locations. Those directories are local data and must not be published.
